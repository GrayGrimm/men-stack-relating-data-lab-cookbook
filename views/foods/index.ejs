<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Pantry</title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
        <h1>Welcome to my Pantry</h1>


        <% if (pantry.length <=0) {%>
            <p>There is no food in the pantry! Go shopping!</p>
            <% } else { %>
                <ul>
                    <% pantry.forEach(food=> { %>
                        <li>
                            <a href="/users/<%= user._id %>/foods/<%= food._id %>">
                                <%= food.name %>
                            </a> 
                            <a href="/users/<%= user._id %>/foods/<%= food._id %>/edit">   Edit link</a>

                        </li>
                        <% }) %>
                            <% }; %>
                </ul>

                <a href="/users/<%= user._id %>/foods/new"> Add a new ingrediant to your pantry! </a>
</body>

</html>